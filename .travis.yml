services:
  - postgresql
language: python
python:
  - "3.6"
install:
  - psql -U postgres -c "CREATE USER admin with PASSWORD 'admin';"
  - psql -U admin -c "CREATE DATABASE Test;"
  - cd backend
  - pip install -r requirements.txt
  - python manage.py migrate
script:
  - python manage.py test dashboard
