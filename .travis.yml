services:
  - postgresql
before_script:
  - psql -c "CREATE DATABASE sustainable-city-ci;"
  # - psql -c "CREATE USER admin with PASSWORD 'admin';"
  # - psql -c "GRANT ALL PRIVILEGES ON DATABASE Test TO admin;"
language: python
python:
  - "3.6"
install:
  - cd backend
  - pip install -r requirements.txt
  - python manage.py migrate
script:
  - python manage.py test dashboard
